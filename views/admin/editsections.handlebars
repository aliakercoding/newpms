s<!-- Body -->
<div class="container mt-3">
    <!-- Section Table -->
    <table class="table table-hover border text-center">
        <thead>
            <tr>
                <th scope="col">اسم القسم</th>
                <th scope="col">عدد الأصناف التابعة للقسم</th>
                <th scope="col">عمليات علي القسم</th>
            </tr>
        </thead>
        <tbody class="refreshSection">
            {{#each getsections}}
            <tr>
                <td>{{section_name}}</td>
                <td>{عدد الأصناف التابعة للقسم}</td>
                <td class="d-flex justify-content-center">
                    <a href="/admin/editsections/{{_id}}" class="btn btn-warning ms-2" data-bs-toggle="tooltip"
                        data-bs-placement="bottom" data-bs-title="تعديل بيانات القسم"><i class="bi bi-pencil"></i></a>
                    <form action="/admin/deletesections/{{_id}}?action=DELETE" method="post">
                        <button type="submit" class="btn btn-danger" data-bs-toggle="tooltip" data-bs-placement="bottom"
                            data-bs-html="true" data-bs-title="حذف القسم <b>- لا يمكن الرجوع في هذا الإجراء</b>"><i
                                class="bi bi-trash"></i>
                        </button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <hr>
    <div class="alert alert-dark" role="alert">
        إضافة قسم جديد
    </div>
    <div class="container">
        <div class="row border pt-3 pb-3">
            <div class="col">
                <form action="/admin/listallsections" method="post" class="needs-validation" novalidate>
                    <!-- SECTION NAME -->
                    <div class="input-group mb-3 has-validation">
                        <span class="input-group-text" id="sectionName-addon">إسم القسم الجديد</span>
                        <input name="sectionName" id="sectionName" type="text" value="{{currentsection.section_name}}"
                            class="form-control" placeholder="مثال: أقراص" aria-label="sectionName"
                            aria-describedby="sectionName-addon" required>
                        <input name="sectionID" id="sectionID" value="{{currentsection._id}}" type="hidden"
                            class="form-control" aria-label="sectionID" aria-describedby="catalogID-addon" required>
                        <div class="invalid-feedback">
                            من فضلك أدخل إسم قسم صحيح
                        </div>
                    </div>
                    <div class="text-center">
                        <button class="btn btn-warning" id="sectionUpdater" type="submit">تعديل بيانات القسم</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

</div>